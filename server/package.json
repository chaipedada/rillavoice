{
  "devDependencies": {
    "@types/node": "^20.4.5",
    "gts": "^5.0.0",
    "typescript": "~5.1.6"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^5.4.0",
    "express": "^4.18.2"
  }
}
